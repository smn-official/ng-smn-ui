import { UiTabContentDirective } from './tab-content.directive';

describe('TabContentDirective', () => {
  it('should create an instance', () => {
    const directive = new UiTabContentDirective();
    expect(directive).toBeTruthy();
  });
});
